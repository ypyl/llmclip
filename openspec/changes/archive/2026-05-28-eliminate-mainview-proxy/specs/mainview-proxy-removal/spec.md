## ADDED Requirements

<!-- No new requirements — this is a pure refactoring of view access patterns. MainController now accesses sub-views directly instead of through MainView proxy methods. All existing behavior is preserved unchanged. -->
