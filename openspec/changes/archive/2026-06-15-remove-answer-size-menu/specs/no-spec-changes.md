## ADDED Requirements

_None — this is a removal-only change with no new capabilities._

## MODIFIED Requirements

_None — no existing spec-level requirements change. The LLM calling behavior is identical to the prior "Default" answer size mode (no verbosity instruction injected)._

## REMOVED Requirements

_None — the answer-size feature has no corresponding spec in `openspec/specs/`. There are no requirement-level artifacts to deprecate._
