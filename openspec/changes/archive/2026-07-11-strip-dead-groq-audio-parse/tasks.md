## 1. GroqAudioProvider — strip dead ParseResponse

- [x] 1.1 Delete the entire `ParseResponse` method (and its 7-line comment block) from `GroqAudioProvider.ahk`
- [x] 1.2 Run `main.ahk`, test audio generation — verify no errors from missing method
