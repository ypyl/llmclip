## 1. Simplify the toolbar

- [x] 1.1 In `ui.html`, trim the `toolbarButtons` array in the OverType editor initialization — remove all formatting buttons (bold, italic, code, link, h1, h2, h3, bulletList, quote) and all separators, keeping only the cancel and save buttons

## 2. Verification

- [ ] 2.1 Run `main.ahk`, double-click a message in the history list, verify the editor toolbar shows only Cancel and Save buttons *(manual verification)*
- [ ] 2.2 Click Cancel, verify the editor closes without changes *(manual verification)*
- [ ] 2.3 Edit text and click Save, verify the message is updated *(manual verification)*
